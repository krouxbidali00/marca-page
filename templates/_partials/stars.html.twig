{# Static star row. Params: value (0..5), size_class (optional, e.g. 'stars--sm', 'fs-5') #}
<span class="stars {{ size_class|default('') }}">
    {%- for i in 1..5 -%}
        <i class="bi {{ i <= value ? 'bi-star-fill' : 'bi-star' }}"></i>
    {%- endfor -%}
</span>
