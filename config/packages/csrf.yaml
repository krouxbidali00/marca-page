# Classic, session-based CSRF protection for forms and logins/logouts.
# Tokens are rendered server-side so the app works without JavaScript.
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection: ~
